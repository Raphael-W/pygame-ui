## Pygame UI
This is a library for creating easy, simple UI in pygame. It integrates easily within your main game loop and can be customised to your preference

## Todo
- Make a new container class
  - When width/height is not set, it uses its children's dimensions
  - Add a padding value
- Make a new layer class
  - Make the elements position based on other elements
- Fix mouse cursor setting - should be a get_cursor() call to hovered element